# --- Core UI ---
PyQt6>=6.6.0

# --- Speech-to-Text (local, free, fast) ---
faster-whisper>=1.0.0

# --- System audio loopback capture (Windows WASAPI) ---
soundcard>=0.4.3
numpy>=1.24.0

# --- LLM providers ---
groq>=0.11.0           # cloud (free tier, blazing fast)
ollama>=0.3.0          # local model client

# --- Global hotkeys ---
pynput>=1.7.6

# --- Config / env ---
python-dotenv>=1.0.0

# --- Resume / JD import (pure-Python wheels, no compiler) ---
pypdf>=4.0.0
python-docx>=1.1.0
